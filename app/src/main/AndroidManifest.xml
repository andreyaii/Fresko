<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFresko"
        tools:targetApi="31">

        <!-- 1. SPLASH SCREEN (Keep as Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MyFresko">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. LOGIN SCREEN (New) -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.MyFresko" />

        <!-- 3. REGISTER SCREEN (New) -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.MyFresko" />

        <!-- 4. EXISTING ACTIVITIES -->
        <activity
            android:name=".home.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".addfood.AddFoodActivity"
            android:exported="false" />

        <activity
            android:name=".history.HistoryActivity"
            android:exported="false" />

        <activity
            android:name=".home.InsightsActivity"
            android:exported="false" />

        <activity
            android:name=".home.FoodDetailActivity"
            android:exported="false" />

        <activity android:name=".home.CategoryActivity" />

    </application>
</manifest>